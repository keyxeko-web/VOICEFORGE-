# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository (VoiceForge) is newly initialized and currently empty. No source files, configuration, or build tooling have been committed yet. This CLAUDE.md should be updated as soon as the project structure is established.

## Project Name

**VoiceForge** — based on the repository name, this is expected to be a voice/audio-related application or tool. Update this file once the tech stack, architecture, and workflows are decided and scaffolded.

## Getting Started (once scaffolded)

When the project is set up, document the following here:

- **Install dependencies**: e.g. `npm install` / `pip install -r requirements.txt`
- **Run the dev server**: e.g. `npm run dev`
- **Build**: e.g. `npm run build`
- **Lint**: e.g. `npm run lint`
- **Run tests**: e.g. `npm test` or `pytest`
- **Run a single test**: e.g. `npm test -- --testNamePattern="..."` or `pytest tests/test_foo.py::test_bar`

## Development Branch

Active development should target the branch specified in the session context. The `main` branch should only receive merged, reviewed changes.
